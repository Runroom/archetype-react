import styled from 'styled-components';

const ScreenReader = styled.span`
  height: 1px;
  left: -999999px;
  overflow: hidden;
  position: absolute;
  top: auto;
  width: 1px;
`;

export default ScreenReader;
